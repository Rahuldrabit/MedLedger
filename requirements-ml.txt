# Note: Heavy ML dependencies (PyTorch, TensorFlow) have been separated to requirements-ml.txt
# These can be installed separately when needed for Phase 2 (Federated Learning)

# Federated Learning (Phase 2)
torch>=2.1.1
# TensorFlow: skip install on Python 3.14+ where wheels may be unavailable
# Install manually on a supported Python version if needed.
tensorflow>=2.15.0; python_version < '3.14'
numpy>=1.26.2
pandas>=2.1.3
scikit-learn>=1.3.2

# Additional blockchain utilities
web3>=6.11.3
ecdsa>=0.18.0

# Optional: Jupyter for development
jupyter>=1.0.0
ipython>=8.18.1
